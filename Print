#include<stdio.h>
#define LIM 100
int main()
{
	int a[LIM];
	int i = 0, j = 0;
	int c = 0;
	for (i = 0; (c = getchar()) != '0'; i++)
	{
		scanf_s("%d", a[i]);
	}
	for (i = 0; i < sizeof(a) / sizeof(a[0]); i++)
	{

		printf("%d ", a[i]);
	}


	return 0;
}
